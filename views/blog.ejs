<!DOCTYPE html>
<html lang="en">

<head>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-LN+7fdVzj6u52u30Kp6M/trliBMCMKTyK833zpbD+pXdCLuTusPj697FH4R/5mcr" crossorigin="anonymous">
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog Post</title>
</head>

<body>

    <%- include('./partials/nav') %>
        <div>
            <h1 style="margin: 10px;">
                <%= locals.blog.title %>
            </h1>
            <img src="/<%= blog.coverImageURL %>" style="width: 500px;">
            <pre> <%= blog.body %></pre>
        </div>
        <div class="mt-3 flex column">
            <h6>Created by : </h6>
            <h6>
                <img src="<%= blog.createdBy.profileImageURL %>" style="width: 50px; height: 50px; border-radius: 50%;">
                <%= blog.createdBy.fullName %>
            </h6>
        </div>
        <div class="mt-3">
            <h2>Comments</h2>
            <% if(locals.comments){ %>
                <% comments.forEach(comment=> { %>
                    <div class="mt-3 flex column">
                        <div>
                            <img src="<%= comment.createdBy.profileImageURL %>"
                                style="width: 50px; height: 50px; border-radius: 50%;">
                            <%= comment.createdBy.fullName %>

                                <span style="margin: 5px;">
                                    : <%= "   " + comment.body %>
                                </span>
                        </div>

                        <% })} if(locals.user) { %>
                            <div class="mt-3">
                                <h4 style="margin: 5px;">Add a Comment :</h4>

                                <form action="/blog/<%= blog._id %>/comment" method="post" style="margin: 10px;">
                                    <div class="mt-3 mb-3">
                                        <input name="body" type="text" class="form-control" id="exampleInputEmail1"
                                            aria-describedby="emailHelp">
                                    </div>
                                    <button type="submit" class="btn btn-primary">Submit</button>
                                </form>
                            </div>
                            <% } %>
                    </div>
                    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/js/bootstrap.bundle.min.js"
                        integrity="sha384-ndDqU0Gzau9qJ1lfW4pNLlhNTkCfHzAVBReH9diLvGRem5+R9g2FzA8ZGN954O5Q"
                        crossorigin="anonymous"></script>
</body>

</html>